[project]
name = "tts-compare"
version = "0.1.0"
description = "Compare open-source TTS models side by side"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "soundfile>=0.13.1",
    "textual>=3.0.0",
    "plotly>=6.0.0",
]
